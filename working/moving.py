import numpy as np

def moving (timestep): 
juvenile_x = np.zeros (nr_agents)
juvenile_y = np.zeros (nr_agents)
    if barbel = juvenile: 
        if juvenile.local_flow != sheltered_flow: 
            # how do i loop over the different juveniles? 
            juvenile_x [i], juvenile_y [i] = coordinatelist_fieldloc (self, fieldcondition_prop, point_propset, resolution, xmin, ymin, 1) # 1 agent to assign the coordinate to 
        else: 
            coords = get_space_domain(timestep)
            x, y = coords.xcoord, coords.ycoord
    if barbel = adult: 
        if adult.local_flow != spawning_grounds: 
            x,y = coordinatelist_fieldloc (self, fieldspawningground_prop, point_propset, resolution, xmin, ymin, nragents)
        else: 
            coords = get_space_domain(timestep)
            x, y = coords.xcoord, coords.ycoord       